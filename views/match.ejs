<!DOCTYPE html>
<html>
  <head>
    <title><%= config.title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script type="text/javascript" src="/javascript/lib/jquery-2.1.1.min.js"></script>
    <script type="text/javascript" src="/javascript/lib/bootstrap.min.js"></script>
    <script type="text/javascript" src="/javascript/match.js"></script>
  </head>
  <body>
  
    <div class="container">
      <div class="jumbotron">
        <a href="/"><h1><img src="/images/logo.png" class="logo" alt="Logo"> <%= config.title %></h1></a>
      </div>
      
      
      <div role="tabpanel">
        <ul id="myTab" class="nav nav-tabs nav-justified" role="tablist">
          <li role="presentation" class="active"><a href="#friends" role="tab" data-toggle="tab" aria-controls="friends" aria-expanded="true">From my Friends List</a></li>
          <li role="presentation" class=""><a href="#nearby" class="tab-nearby" role="tab" data-toggle="tab" aria-controls="nearby" aria-expanded="false">Drinkers Nearby</a></li>
        </ul>
        <div id="myTabContent" class="tab-content">
          <div role="tabpanel" class="tab-pane fade active in" id="friends">
            <p>Select two users to match</p>
            <form class="form" role="form" action="/match/result" >  
              <div class="form-group">
                <label class="sr-only" for="chooseFriend">From Friends List</label>
                <select class="form-control input-lg" id="chooseFriend">
                  <option value="<%= me %>"><%= me %> (me)</option>
                  <% friends.forEach(function(friend){ %>
                    <option value="<%= friend.username %>"><%= friend.first_name %> <%= friend.last_name %></option>
                    <% }); %>
                    <option value="other">Other</option>
                  </select>
                </div>
                <div class="form-group other">
                  Or
                  <label class="sr-only" for="a">Username</label>
                  <input name="a" type="text" class="form-control input-lg" id="a" placeholder="Untappd Username">
                </div>
                <div class="form-group">
                  
                </div>
                
                <p>and</p>
                
                <div class="form-group">
                  <label class="sr-only" for="chooseFriend">From Friends List</label>
                  <select class="form-control input-lg" id="chooseFriend">
                    <% friends.forEach(function(friend){ %>
                      <option value="<%= friend.username %>"><%= friend.first_name %> <%= friend.last_name %></option>
                      <% }); %>
                      <option value="other">Other</option>
                    </select>
                  </div>
                  <div class="form-group other">
                    Or
                    <label class="sr-only" for="b">Username</label>
                    <input name="b" type="text" class="form-control input-lg" id="b" placeholder="Untappd Username">
                  </div>
                  <br />
                  <button type="submit" class="btn btn-friends btn-primary btn-lg col-md-12">
                    <span>Match me! &raquo;</span><br />
                    <small>Matching may take some time, depending on the number of check-ins!</small></button>
            </form>
          </div>
          <div role="tabpanel" class="tab-pane fade" id="nearby">
            <form class="form" role="form" action="/match/nearby" >
              <div class="form-group">
                <label class="sr-only" for="a">Latitude</label>
                <input name="lat" type="text" class="form-control input-lg" id="a" placeholder="Latitude">
              </div>
              <div class="form-group">
                <label class="sr-only" for="a">Username</label>
                <input name="long" type="text" class="form-control input-lg" id="a" placeholder="Longitude">
              </div>
              <div class="form-group">
                <label class="sr-only" for="a">Radius</label>
                <select name="radius" class="form-control input-lg">
                  <option value="1">1 Mile</option>
                  <option value="10" selected>10 Miles</option>
                  <option value="50">50 Miles</option>
                  <option value="-1">Many Miles</option>
                </select>
                
              </div>
              <button type="submit" class="btn btn-primary btn-nearby btn-lg col-md-12">
                <span>Find matches near me!</span>
              </button>
            </form>
          </div>
        </div>
      </div>
      

   
    </div><!-- /container -->
    
    <div class="footer">
      <div class="container">
        <small>
        <%= config.title %> is created by <a href="http://www.cianclarke.com">Cian Clarke</a>, and is in no way affiliated with Untappd.<br />
        <%= config.title %> requires access to your Untappd Account. We won't do anything nasty like post to your feed, or recommend an AA meeting - promise.
        </small>
      </div>
    </div>
  </body>
</html>
